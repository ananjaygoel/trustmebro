---
title: "Clustering Unstructured Text with LLM Embeddings and HDBSCAN"
excerpt: "The current era of Generative AI seems to primarily focus on chat interfaces and prompts, but the range of applications of large language..."
category: "ai"
date: 2026-06-23
publishedAt: "2026-06-23T19:49:50.663Z"
image: "https://machinelearningmastery.com/wp-content/uploads/2026/06/mlm-clustering-unstructured-text-with-llm-embeddings-and-hdbscan-feature.png"
author: "no cap correspondent 🧢"
source: "ML Mastery"
featured: false
---

## What's Happening

Real talk: The current era of Generative AI seems to primarily focus on chat interfaces and prompts, but the range of applications of large language models , or LLMs for short, is not limited to just that.

Clustering Unstructured Text with LLM Embeddings and HDBSCAN By Iván Palomares Carrascosa on in Language Models 0 Post In this article, you will learn how to build a text clustering pipeline language model embeddings with HDBSCAN, a density-based clustering algorithm, to automatically discover topics in unlabeled text data. Topics we will cover include: How to generate text embeddings for raw documents using a pre-trained sentence-transformers model. (we're not making this up)

How to reduce the dimensionality of those embeddings with UMAP to prepare them for clustering.

## The Details

How to apply HDBSCAN to automatically discover topic clusters and visualize the results. Indeed, one of their most powerful downstream abilities consists of turning raw, messy, unstructured text into semantically rich mathematical representations called embeddings .

Once thats done, we can use these text representations for a variety of ML use cases, with clustering being no exception. In particular, embeddings can be combined with advanced, density-based clustering techniques like HDBSCAN , allowing so for the discovery of hidden topics, patterns, or categories in your collection of text documents: all without the need for prior labeling.

## Why This Matters

This article shows how to construct a text-based clustering pipeline from scratch. We will use a freely available dataset containing text instances, as well as an open-source LLM that has been trained for generating embeddings i. The icing on the cake: well use free and handy, modern Python libraries providing implementations of clustering algorithms like HDBSCAN.

As AI capabilities expand, we're seeing more announcements like this reshape the industry.

## The Bottom Line

The icing on the cake: well use free and handy, modern Python libraries providing implementations of clustering algorithms like HDBSCAN.

Is this a W or an L? You decide.
